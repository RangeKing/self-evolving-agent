interface:
  display_name: "Self-Evolving Agent"
  short_description: "Phase-aware capability evolution with canonical records"
  default_prompt: "Use $self-evolving-agent to classify the task into task_light, task_full, agenda_review, or promotion_review mode, retrieve only the required records, preserve legacy self-improving-agent logs as read-only evidence, and write new evidence through the evolution runtime."
